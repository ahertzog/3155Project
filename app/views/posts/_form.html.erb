<%= form_for([@club, @club.posts.build]) do |f| %>
 
    
    <p>
        Title
        <%= f.text_field :title %>
    </p>
    
    <p>
        Body
        <%= f.text_area :body %>
    </p>
    
    <p>
        <%= f.submit %>
    </p>
<%end%>